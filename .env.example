# Required before publication. Use the real HTTPS origin; no trailing path.
NEXT_PUBLIC_SITE_URL=
# Real email address. Contact remains noindex until configured.
CONTACT_EMAIL=
# Optional: exact Search Console verification token (not the full meta tag).
GOOGLE_SITE_VERIFICATION=
# Optional: reserve identified advertising space; no ad scripts are loaded.
ADS_ENABLED=false
